# Core dependencies - no external packages needed for basic examples
# asyncio, dataclasses, typing are built-in to Python 3.7+

# API framework (optional, for API server example)
fastapi>=0.100.0
uvicorn>=0.23.0
strawberry-graphql>=0.200.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
httpx>=0.24.0

# Optional: Real database drivers
# neo4j>=5.0.0
